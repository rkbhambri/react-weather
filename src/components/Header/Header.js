import React from 'react';
import './Header.css';

const Header = (props) => {
    return (
        <div className="header d-flex justify-content-between">
            <p>React Weather</p>
        </div>
    );
};

export default Header;
